/* ============================================
   Dev Place - MAD MAN | تصميم احترافي أسطوري
   ============================================ */

:root {
    --bg: #0a0a0f;
    --bg2: #111118;
    --surface: #1a1a24;
    --surface2: #222230;
    --text: #f0f0f5;
    --text2: #a0a0b8;
    --text3: #606078;
    --border: #2a2a38;
    --border-light: #333348;
    --primary: #6c5ce7;
    --primary-glow: #a78bfa;
    --primary-dark: #5a4bd1;
    --accent: #00d2ff;
    --accent2: #f472b6;
    --gradient-1: linear-gradient(135deg, #6c5ce7, #00d2ff);
    --gradient-2: linear-gradient(135deg, #f472b6, #6c5ce7);
    --gradient-3: linear-gradient(135deg, #00d2ff, #6c5ce7, #f472b6);
    --glass: rgba(255,255,255,0.03);
    --glass-border: rgba(255,255,255,0.06);
    --glass-hover: rgba(255,255,255,0.06);
    --shadow: 0 4px 30px rgba(0,0,0,0.3);
    --shadow-lg: 0 20px 60px rgba(0,0,0,0.5);
    --shadow-glow: 0 0 40px rgba(108,92,231,0.3);
    --radius-sm: 10px;
    --radius: 16px;
    --radius-lg: 24px;
    --radius-xl: 32px;
    --transition: 0.4s cubic-bezier(0.25, 0.8, 0.25, 1.2);
    --font: 'Cairo', sans-serif;
    --font-display: 'Space Grotesk', sans-serif;
}

[data-theme="light"] {
    --bg: #f5f5fa;
    --bg2: #eeeef5;
    --surface: #ffffff;
    --surface2: #f8f8fc;
    --text: #1a1a24;
    --text2: #555568;
    --text3: #909098;
    --border: #e0e0ea;
    --border-light: #f0f0f5;
    --glass: rgba(0,0,0,0.02);
    --glass-border: rgba(0,0,0,0.05);
    --shadow: 0 4px 20px rgba(0,0,0,0.06);
    --shadow-lg: 0 20px 50px rgba(0,0,0,0.1);
    --shadow-glow: 0 0 40px rgba(108,92,231,0.15);
}

*,*::before,*::after{margin:0;padding:0;box-sizing:border-box}
html{scroll-behavior:smooth;scroll-padding-top:90px}
body{font-family:var(--font);background:var(--bg);color:var(--text);line-height:1.8;overflow-x:hidden;transition:var(--transition)}
a{text-decoration:none;color:inherit}
ul{list-style:none}
img{max-width:100%}

/* ============ مؤشر ============ */
.cursor{position:fixed;width:30px;height:30px;border:2px solid var(--primary);border-radius:50%;pointer-events:none;z-index:99999;transition:0.15s ease;transform:translate(-50%,-50%);opacity:0.7}
.cursor-dot{position:fixed;width:6px;height:6px;background:var(--primary-glow);border-radius:50%;pointer-events:none;z-index:99999;transform:translate(-50%,-50%);box-shadow:0 0 15px var(--primary-glow)}
@media(max-width:768px){.cursor,.cursor-dot{display:none}}

/* ============ جسيمات ============ */
.particles{position:fixed;inset:0;z-index:0;pointer-events:none;opacity:0.4}
.particle{position:absolute;border-radius:50%;background:var(--primary);animation:floatUp linear infinite}
@keyframes floatUp{0%{transform:translateY(110vh) scale(0);opacity:0}10%{opacity:1}90%{opacity:1}100%{transform:translateY(-10vh) scale(1);opacity:0}}

/* ============ هيدر ============ */
.header{position:fixed;top:20px;left:50%;transform:translateX(-50%);z-index:1000;width:calc(100% - 40px);max-width:1200px;transition:var(--transition)}
.header.scrolled{top:0;width:100%;max-width:100%;border-radius:0}
.header-inner{background:var(--surface);border:1px solid var(--border);border-radius:var(--radius-xl);padding:12px 20px;display:flex;align-items:center;justify-content:space-between;backdrop-filter:blur(20px);box-shadow:var(--shadow);transition:var(--transition)}
.header.scrolled .header-inner{border-radius:0;border-left:none;border-right:none;border-top:none}
.brand{display:flex;align-items:center;gap:12px}
.brand-icon{width:42px;height:42px;background:var(--gradient-1);border-radius:var(--radius-sm);display:flex;align-items:center;justify-content:center;color:#fff;font-size:1.2rem;box-shadow:var(--shadow-glow)}
.brand-text{font-family:var(--font-display);font-size:1.4rem;font-weight:700;color:var(--text)}
.brand-text span{background:var(--gradient-1);-webkit-background-clip:text;-webkit-text-fill-color:transparent}
.nav{display:flex;align-items:center;gap:6px}
.nav-item{padding:10px 18px;border-radius:50px;font-weight:600;font-size:0.9rem;color:var(--text2);transition:var(--transition);position:relative;overflow:hidden}
.nav-item:hover,.nav-item.active{color:#fff;background:var(--gradient-1);box-shadow:var(--shadow-glow)}
.nav-item-special{background:var(--glass);border:1px solid var(--primary);color:var(--primary)}
.nav-item-special:hover{background:var(--primary);color:#fff}
.header-actions{display:flex;align-items:center;gap:10px}
.action-btn{width:42px;height:42px;border-radius:50%;border:1px solid var(--border);background:var(--surface);color:var(--text2);cursor:pointer;display:flex;align-items:center;justify-content:center;transition:var(--transition);font-size:0.95rem;font-weight:700}
.action-btn:hover{border-color:var(--primary);color:var(--primary);box-shadow:var(--shadow-glow)}
.menu-btn{display:none}
.btn-glass{padding:10px 20px;border-radius:50px;border:1px solid var(--border);background:var(--glass);color:var(--text);cursor:pointer;display:flex;align-items:center;gap:8px;font-weight:600;font-family:inherit;transition:var(--transition);font-size:0.9rem}
.btn-glass:hover{background:var(--glass-hover);border-color:var(--primary);box-shadow:var(--shadow-glow)}
.btn-glow{padding:14px 28px;border-radius:50px;border:none;background:var(--gradient-1);color:#fff;cursor:pointer;display:flex;align-items:center;gap:10px;font-weight:700;font-family:inherit;transition:var(--transition);box-shadow:var(--shadow-glow);font-size:0.95rem}
.btn-glow:hover{transform:translateY(-3px);box-shadow:0 0 50px rgba(108,92,231,0.5)}
.btn-glow.danger{background:linear-gradient(135deg,#ef4444,#dc2626);box-shadow:0 0 30px rgba(239,68,68,0.3)}
.btn-full{width:100%;justify-content:center}
.user-menu{position:relative}
.user-avatar-sm{width:32px;height:32px;border-radius:50%;background:var(--gradient-1);display:flex;align-items:center;justify-content:center;color:#fff;font-weight:700;font-size:0.8rem}
.user-dropdown{position:absolute;top:calc(100% + 12px);left:0;background:var(--surface);border:1px solid var(--border);border-radius:var(--radius);padding:8px;min-width:200px;opacity:0;visibility:hidden;transform:translateY(-10px);transition:var(--transition);box-shadow:var(--shadow-lg);z-index:1001}
.user-menu:hover .user-dropdown{opacity:1;visibility:visible;transform:translateY(0)}
.user-dropdown a{display:flex;align-items:center;gap:10px;padding:10px 14px;border-radius:var(--radius-sm);color:var(--text2);transition:var(--transition)}
.user-dropdown a:hover{background:var(--glass);color:var(--primary)}
.user-dropdown a.danger{color:#ef4444}
.user-dropdown a.danger:hover{background:rgba(239,68,68,0.1)}
.dropdown-divider{height:1px;background:var(--border);margin:6px 0}

/* ============ شريط التقدم ============ */
.progress-bar{position:fixed;top:0;left:0;height:3px;background:var(--gradient-1);z-index:1001;transition:width 0.1s;border-radius:0 3px 3px 0}

/* ============ Hero ============ */
.hero{min-height:100vh;display:flex;align-items:center;justify-content:center;padding:120px 20px 80px;position:relative;overflow:hidden}
.hero-bg{position:absolute;inset:0;z-index:0}
.hero-orb{position:absolute;border-radius:50%;filter:blur(100px);opacity:0.3}
.hero-orb-1{width:500px;height:500px;background:var(--primary);top:-100px;right:-100px;animation:orbFloat 8s ease-in-out infinite}
.hero-orb-2{width:350px;height:350px;background:var(--accent);bottom:-50px;left:-50px;animation:orbFloat 10s ease-in-out 2s infinite}
.hero-orb-3{width:250px;height:250px;background:var(--accent2);top:50%;left:50%;animation:orbFloat 7s ease-in-out 4s infinite}
@keyframes orbFloat{0%,100%{transform:translate(0,0) scale(1)}33%{transform:translate(30px,-30px) scale(1.1)}66%{transform:translate(-20px,20px) scale(0.9)}}
.hero-content{position:relative;z-index:1;text-align:center;max-width:750px}
.hero-badge{display:inline-flex;align-items:center;gap:8px;padding:8px 20px;background:var(--glass);border:1px solid var(--border);border-radius:50px;font-size:0.85rem;color:var(--primary);margin-bottom:28px;font-weight:600}
.hero-badge .dot{width:8px;height:8px;border-radius:50%;background:var(--accent);animation:pulse 2s infinite}
@keyframes pulse{0%,100%{opacity:1}50%{opacity:0.3}}
.hero-title{font-size:5rem;font-weight:900;line-height:1.1;margin-bottom:16px;font-family:var(--font-display)}
.hero-title .gradient-text{background:var(--gradient-3);-webkit-background-clip:text;-webkit-text-fill-color:transparent;background-clip:text}
.hero-subtitle{font-size:1.3rem;color:var(--text2);margin-bottom:10px;font-weight:500}
.hero-desc{color:var(--text3);margin-bottom:40px;max-width:500px;margin-left:auto;margin-right:auto;font-size:1rem}
.hero-buttons{display:flex;gap:14px;justify-content:center;flex-wrap:wrap;margin-bottom:50px}
.hero-stats{display:flex;gap:50px;justify-content:center;flex-wrap:wrap}
.stat-item{text-align:center}
.stat-number{font-size:2.5rem;font-weight:900;font-family:var(--font-display);background:var(--gradient-1);-webkit-background-clip:text;-webkit-text-fill-color:transparent}
.stat-label{font-size:0.9rem;color:var(--text3);font-weight:500}

/* ============ أقسام ============ */
section{padding:100px 0;position:relative;z-index:1}
.container{max-width:1200px;margin:0 auto;padding:0 24px}
.section-label{display:inline-flex;align-items:center;gap:8px;padding:6px 16px;background:var(--glass);border:1px solid var(--border);border-radius:50px;font-size:0.8rem;color:var(--primary);margin-bottom:16px;font-weight:600;letter-spacing:1px}
.section-title{font-size:2.8rem;font-weight:900;margin-bottom:12px;font-family:var(--font-display)}
.section-title span{background:var(--gradient-1);-webkit-background-clip:text;-webkit-text-fill-color:transparent}
.section-sub{color:var(--text3);max-width:500px;margin:0 auto 60px;font-size:1rem}
.text-center{text-align:center}
.mx-auto{margin-left:auto;margin-right:auto}

/* ============ عن المطور ============ */
.about-grid{display:grid;grid-template-columns:1fr 1fr;gap:60px;align-items:center}
.about-visual{position:relative;display:flex;justify-content:center}
.about-avatar-wrap{position:relative}
.about-avatar{width:280px;height:280px;border-radius:50%;background:var(--gradient-1);display:flex;align-items:center;justify-content:center;font-size:7rem;color:#fff;box-shadow:0 0 80px rgba(108,92,231,0.4);position:relative}
.about-avatar-ring{position:absolute;inset:-20px;border-radius:50%;border:2px dashed var(--primary);opacity:0.3;animation:spin 25s linear infinite}
@keyframes spin{to{transform:rotate(360deg)}}
.about-floating-card{position:absolute;bottom:0;left:-40px;background:var(--surface);border:1px solid var(--border);border-radius:var(--radius);padding:16px 20px;box-shadow:var(--shadow-lg);animation:floatCard 4s ease-in-out infinite;white-space:nowrap}
.about-floating-card i{color:var(--accent);margin-left:6px}
@keyframes floatCard{0%,100%{transform:translateY(0)}50%{transform:translateY(-15px)}}
.about-info h3{font-size:1.8rem;margin-bottom:16px;font-family:var(--font-display)}
.about-info p{color:var(--text2);margin-bottom:12px;line-height:1.9}
.info-cards{display:grid;grid-template-columns:1fr 1fr;gap:14px;margin-top:24px}
.info-card{background:var(--surface);border:1px solid var(--border);border-radius:var(--radius);padding:18px;display:flex;align-items:center;gap:14px;transition:var(--transition)}
.info-card:hover{border-color:var(--primary);box-shadow:var(--shadow-glow)}
.info-card-icon{width:44px;height:44px;border-radius:var(--radius-sm);background:var(--glass);display:flex;align-items:center;justify-content:center;color:var(--primary);font-size:1.1rem}
.info-card h4{font-size:0.85rem;color:var(--text3);margin-bottom:2px}
.info-card span{font-weight:600;font-size:0.9rem}
.copy-btn{background:none;border:none;color:var(--primary);cursor:pointer;margin-right:6px;transition:var(--transition)}
.copy-btn:hover{color:var(--accent)}
.skills-wrap{display:flex;flex-wrap:wrap;gap:8px;margin-top:24px}
.skill-tag{padding:8px 16px;background:var(--glass);border:1px solid var(--border);border-radius:50px;font-size:0.8rem;font-weight:600;color:var(--text2);transition:var(--transition)}
.skill-tag:hover{background:var(--primary);color:#fff;border-color:var(--primary);box-shadow:var(--shadow-glow)}

/* ============ المشاريع ============ */
.projects-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(320px,1fr));gap:24px}
.project-card{background:var(--surface);border:1px solid var(--border);border-radius:var(--radius-lg);padding:32px;transition:var(--transition);position:relative;overflow:hidden}
.project-card::before{content:'';position:absolute;top:0;left:0;right:0;height:3px;background:var(--gradient-1)}
.project-card:hover{transform:translateY(-8px);box-shadow:var(--shadow-lg),var(--shadow-glow);border-color:var(--primary)}
.project-card-icon{width:60px;height:60px;border-radius:var(--radius);background:var(--glass);display:flex;align-items:center;justify-content:center;font-size:1.5rem;color:var(--primary);margin-bottom:20px;border:1px solid var(--border)}
.project-card h3{font-family:var(--font-display);margin-bottom:8px;font-size:1.2rem}
.project-card p{color:var(--text3);font-size:0.9rem;margin-bottom:16px}
.project-meta{display:flex;gap:20px;margin-bottom:16px;font-size:0.8rem;color:var(--text3)}
.project-tags{display:flex;gap:6px;flex-wrap:wrap;margin-bottom:20px}
.project-tags span{padding:5px 10px;background:var(--glass);border:1px solid var(--border);border-radius:50px;font-size:0.7rem;color:var(--text2);font-weight:600}
.badge-sm{padding:4px 10px;border-radius:50px;font-size:0.7rem;font-weight:600}
.badge-done{background:rgba(16,185,129,0.1);color:#10b981;border:1px solid rgba(16,185,129,0.3)}
.badge-progress{background:rgba(245,158,11,0.1);color:#f59e0b;border:1px solid rgba(245,158,11,0.3)}
.project-actions{display:flex;gap:8px;flex-wrap:wrap}
.btn-sm-glass{padding:8px 16px;border-radius:50px;border:1px solid var(--border);background:var(--glass);color:var(--text2);cursor:pointer;font-family:inherit;transition:var(--transition);font-size:0.85rem;display:flex;align-items:center;gap:6px}
.btn-sm-glass:hover{border-color:var(--primary);color:var(--primary)}
.btn-sm-glass.danger:hover{border-color:#ef4444;color:#ef4444;background:rgba(239,68,68,0.05)}
.empty-state{padding:80px 20px;text-align:center;color:var(--text3)}
.empty-state i{font-size:4rem;color:var(--primary);opacity:0.3;margin-bottom:20px}

/* ============ تواصل ============ */
.contact-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(300px,1fr));gap:24px}
.contact-card{background:var(--surface);border:1px solid var(--border);border-radius:var(--radius-lg);padding:36px;text-align:center;transition:var(--transition)}
.contact-card:hover{transform:translateY(-5px);box-shadow:var(--shadow-lg);border-color:var(--primary)}
.contact-card-icon{width:70px;height:70px;border-radius:50%;background:var(--glass);display:flex;align-items:center;justify-content:center;font-size:1.8rem;color:var(--primary);margin:0 auto 20px;border:1px solid var(--border)}
.contact-card h3{font-family:var(--font-display);margin-bottom:8px}
.contact-card p{color:var(--text2);margin-bottom:16px}

/* ============ لوحة تحكم ============ */
.dashboard-layout{display:flex;min-height:100vh;padding-top:100px}
.dashboard-sidebar{width:270px;background:var(--surface);border-left:1px solid var(--border);padding:24px;position:fixed;top:90px;bottom:20px;right:20px;border-radius:var(--radius-xl);overflow-y:auto;transition:var(--transition);z-index:50}
.sidebar-header{text-align:center;margin-bottom:30px}
.sidebar-logo{font-family:var(--font-display);font-size:1.3rem;font-weight:700}
.sidebar-logo span{background:var(--gradient-1);-webkit-background-clip:text;-webkit-text-fill-color:transparent}
.sidebar-nav a{display:flex;align-items:center;gap:12px;padding:12px 16px;border-radius:var(--radius);color:var(--text2);margin-bottom:4px;transition:var(--transition);font-weight:500}
.sidebar-nav a:hover,.sidebar-nav a.active{background:var(--glass);color:var(--primary)}
.sidebar-nav a i{width:20px;text-align:center}
.dashboard-main{flex:1;margin-right:310px;padding:0 30px 60px}
.welcome-card{background:var(--gradient-1);color:#fff;padding:32px;border-radius:var(--radius-xl);margin-bottom:30px;display:flex;align-items:center;justify-content:space-between;gap:20px;box-shadow:var(--shadow-glow)}
.welcome-card h2{font-family:var(--font-display)}
.stats-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(200px,1fr));gap:18px;margin-bottom:30px}
.stat-card-db{background:var(--surface);border:1px solid var(--border);border-radius:var(--radius-lg);padding:24px;display:flex;align-items:center;gap:16px;transition:var(--transition)}
.stat-card-db:hover{transform:translateY(-3px);box-shadow:var(--shadow-lg)}
.stat-icon-db{width:50px;height:50px;border-radius:var(--radius);display:flex;align-items:center;justify-content:center;font-size:1.3rem}
.stat-icon-db.purple{background:rgba(108,92,231,0.1);color:var(--primary)}
.stat-icon-db.blue{background:rgba(0,210,255,0.1);color:var(--accent)}
.stat-icon-db.green{background:rgba(16,185,129,0.1);color:#10b981}
.stat-icon-db.pink{background:rgba(244,114,182,0.1);color:var(--accent2)}
.stat-info-db h3{font-family:var(--font-display);font-size:1.8rem}
.stat-info-db p{font-size:0.85rem;color:var(--text3)}
.info-grid{display:grid;grid-template-columns:1fr 1fr;gap:24px}
.info-block{background:var(--surface);border:1px solid var(--border);border-radius:var(--radius-lg);overflow:hidden}
.info-block-header{padding:18px 24px;border-bottom:1px solid var(--border);font-weight:700}
.info-block-body{padding:24px}
.info-row{display:flex;align-items:center;gap:14px;padding:12px 0;border-bottom:1px solid var(--border)}
.info-row:last-child{border-bottom:none}
.info-row-icon{width:40px;height:40px;border-radius:50%;background:var(--glass);display:flex;align-items:center;justify-content:center;color:var(--primary)}
.info-row strong{display:block;font-size:0.8rem;color:var(--text3)}
.info-row span{font-size:0.9rem}

/* ============ صفحات المصادقة ============ */
.auth-page{min-height:100vh;display:flex;align-items:center;justify-content:center;padding:120px 20px 80px;position:relative;z-index:1}
.auth-card{background:var(--surface);border:1px solid var(--border);border-radius:var(--radius-xl);width:100%;max-width:460px;overflow:hidden;box-shadow:var(--shadow-lg)}
.auth-card-header{padding:36px;text-align:center;background:var(--gradient-1);color:#fff}
.auth-logo{font-family:var(--font-display);font-size:2rem;font-weight:700;margin-bottom:6px}
.auth-card-body{padding:36px}

/* ============ مودال ============ */
.modal{position:fixed;inset:0;z-index:2000;display:flex;align-items:center;justify-content:center;opacity:0;visibility:hidden;transition:0.3s ease}
.modal.active{opacity:1;visibility:visible}
.modal-backdrop{position:absolute;inset:0;background:rgba(0,0,0,0.6);backdrop-filter:blur(8px)}
.modal-content{position:relative;background:var(--surface);border:1px solid var(--border);border-radius:var(--radius-xl);width:90%;max-width:440px;padding:40px;box-shadow:var(--shadow-lg);transform:translateY(20px);transition:0.4s ease;max-height:90vh;overflow-y:auto}
.modal.active .modal-content{transform:translateY(0)}
.modal-lg{max-width:550px}
.modal-sm{max-width:380px}
.modal-close{position:absolute;top:16px;left:16px;width:36px;height:36px;border-radius:50%;border:1px solid var(--border);background:var(--surface);color:var(--text2);cursor:pointer;display:flex;align-items:center;justify-content:center;transition:var(--transition);z-index:1}
.modal-close:hover{background:#ef4444;color:#fff;border-color:#ef4444}
.modal-icon{width:64px;height:64px;border-radius:50%;background:var(--glass);display:flex;align-items:center;justify-content:center;font-size:1.6rem;color:var(--primary);margin:0 auto 16px;border:1px solid var(--border)}
.modal-icon.danger{color:#ef4444;background:rgba(239,68,68,0.1);border-color:rgba(239,68,68,0.3)}
.modal h2{text-align:center;font-family:var(--font-display);margin-bottom:6px}
.modal p{text-align:center;color:var(--text3);margin-bottom:24px}
.modal-actions{display:flex;gap:10px;justify-content:center;margin-top:20px}
.input-group{position:relative;margin-bottom:16px}
.input-group i{position:absolute;right:16px;top:50%;transform:translateY(-50%);color:var(--text3)}
.input-group input,.input-group textarea,.input-group select{width:100%;padding:14px 46px 14px 16px;border:1px solid var(--border);border-radius:var(--radius);background:var(--surface2);color:var(--text);font-family:inherit;font-size:0.95rem;transition:var(--transition)}
.input-group textarea{resize:vertical;min-height:90px}
.input-group input:focus,.input-group textarea:focus,.input-group select:focus{outline:none;border-color:var(--primary);box-shadow:0 0 0 4px rgba(108,92,231,0.1)}
.input-row{display:grid;grid-template-columns:1fr 1fr;gap:16px}
.toggle-pass-btn{position:absolute;left:12px;top:50%;transform:translateY(-50%);background:none;border:none;color:var(--text3);cursor:pointer}
.form-error{color:#ef4444;font-size:0.85rem;margin-bottom:12px;min-height:20px}
.form-success{color:#10b981;text-align:center;margin-bottom:12px;display:none;font-weight:600}
.modal-footer-text{text-align:center;margin-top:20px;color:var(--text3);font-size:0.9rem}
.modal-footer-text a{color:var(--primary);font-weight:600}

/* ============ الملف الشخصي ============ */
.profile-page{padding:120px 20px 80px;position:relative;z-index:1}
.profile-card{max-width:600px;margin:0 auto;background:var(--surface);border:1px solid var(--border);border-radius:var(--radius-xl);overflow:hidden;box-shadow:var(--shadow-lg)}
.profile-cover{height:140px;background:var(--gradient-1)}
.profile-avatar-lg{width:110px;height:110px;border-radius:50%;background:var(--surface);display:flex;align-items:center;justify-content:center;font-size:2.5rem;color:var(--primary);margin:-55px auto 0;border:4px solid var(--surface);box-shadow:var(--shadow)}
.profile-body{padding:30px;text-align:center}
.profile-body h2{font-family:var(--font-display)}
.profile-body .role-badge{display:inline-block;padding:4px 14px;border-radius:50px;background:var(--glass);border:1px solid var(--border);font-size:0.8rem;color:var(--primary);margin-bottom:24px}
.profile-details{text-align:right}
.pd-row{display:flex;align-items:center;gap:14px;padding:14px 0;border-bottom:1px solid var(--border)}
.pd-row:last-child{border-bottom:none}
.pd-icon{width:42px;height:42px;border-radius:50%;background:var(--glass);display:flex;align-items:center;justify-content:center;color:var(--primary);flex-shrink:0}
.pd-info strong{display:block;font-size:0.8rem;color:var(--text3)}
.pd-info span{font-size:0.95rem}

/* ============ 404 ============ */
.error-section{min-height:100vh;display:flex;align-items:center;justify-content:center;text-align:center;position:relative;z-index:1;padding:20px}
.error-code{font-size:10rem;font-weight:900;font-family:var(--font-display);background:var(--gradient-1);-webkit-background-clip:text;-webkit-text-fill-color:transparent;line-height:1}
.error-section p{color:var(--text3);margin-bottom:30px;font-size:1.1rem}

/* ============ فooter ============ */
.footer-new{position:relative;z-index:1;background:var(--surface);border-top:1px solid var(--border);padding:60px 24px 20px}
.footer-glow{position:absolute;top:0;left:50%;transform:translateX(-50%);width:300px;height:1px;background:var(--gradient-1)}
.footer-content{max-width:1200px;margin:0 auto;display:grid;grid-template-columns:2fr 1fr 1fr;gap:40px;margin-bottom:40px}
.brand-icon-lg{width:50px;height:50px;border-radius:var(--radius);background:var(--gradient-1);display:flex;align-items:center;justify-content:center;color:#fff;font-size:1.3rem;margin-bottom:16px}
.footer-brand h3{font-family:var(--font-display);margin-bottom:8px}
.footer-brand p{color:var(--text3)}
.footer-links h4,.footer-contact h4{margin-bottom:16px}
.footer-links a{display:block;color:var(--text2);margin-bottom:8px;transition:var(--transition)}
.footer-links a:hover{color:var(--primary)}
.footer-contact p{color:var(--text2);margin-bottom:8px;display:flex;align-items:center;gap:8px}
.footer-contact i{color:var(--primary)}
.footer-bar{max-width:1200px;margin:0 auto;padding-top:20px;border-top:1px solid var(--border);text-align:center;color:var(--text3);font-size:0.9rem}
.footer-bar strong{color:var(--primary)}

/* ============ توست ============ */
.toasts{position:fixed;bottom:30px;left:50%;transform:translateX(-50%);z-index:9999;display:flex;flex-direction:column;gap:8px;align-items:center}
.toast{padding:14px 24px;border-radius:50px;background:var(--surface);color:var(--text);border:1px solid var(--border);font-weight:600;font-size:0.9rem;box-shadow:var(--shadow-lg);animation:toastIn 0.4s ease;display:flex;align-items:center;gap:8px}
.toast.success{border-color:#10b981;color:#10b981}
.toast.error{border-color:#ef4444;color:#ef4444}
@keyframes toastIn{from{opacity:0;transform:translateY(20px)}to{opacity:1;transform:translateY(0)}}

/* ============ تجاوب ============ */
@media(max-width:1100px){
    .nav{position:fixed;top:0;right:-100%;width:300px;height:100vh;background:var(--surface);flex-direction:column;padding:100px 20px 20px;border-left:1px solid var(--border);transition:var(--transition);z-index:99;gap:4px}
    .nav.active{right:0}
    .menu-btn{display:flex}
    .hero-title{font-size:3.5rem}
    .about-grid{grid-template-columns:1fr}
    .dashboard-layout{flex-direction:column}
    .dashboard-sidebar{position:relative;top:0;right:0;bottom:auto;width:100%;border-radius:0;display:flex;overflow-x:auto;padding:14px;gap:8px}
    .dashboard-sidebar a{white-space:nowrap;flex-shrink:0}
    .dashboard-main{margin-right:0}
    .info-grid{grid-template-columns:1fr}
}
@media(max-width:768px){
    .hero-title{font-size:2.5rem}
    .section-title{font-size:2rem}
    .hero-stats{gap:30px}
    .hero-buttons{flex-direction:column;align-items:center}
    .footer-content{grid-template-columns:1fr}
    .info-cards{grid-template-columns:1fr}
    .input-row{grid-template-columns:1fr}
}
@media(max-width:480px){
    .hero-title{font-size:2rem}
    .hero-avatar{width:200px;height:200px;font-size:5rem}
    .stats-grid{grid-template-columns:1fr}
    .projects-grid{grid-template-columns:1fr}
}
